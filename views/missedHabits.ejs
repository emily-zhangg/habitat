<!DOCTYPE html>
<html lang="en">
  <%-include('header',{})%>
  <head>
    <title>Missed Habits</title>
  </head>
  <body>
    <%- include('nav',{user_id:user_id})-%>
    <h1>Here are the habits that you missed</h1>
    <ul class="list-group">
      <%if (habits.length>0){ habits.forEach((habit)=>{%>
      <li class="list-group-item"><%=habit.habit%></li>
      <%})}else{%>
      <p>You have no missed habits</p>
      <%}%>
    </ul>
  </body>
</html>
